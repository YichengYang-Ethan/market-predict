__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env
.pytest_cache/
.ruff_cache/
*.parquet
snapshots/
.DS_Store
# Snapshot JSON lives on the `snapshots` branch, never on main.
# Local builds may produce data/snapshot_*.json for dev — ignore them.
data/snapshot_*.json
